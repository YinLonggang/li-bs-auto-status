# 2026-08-20

## Zeus SDK 接入(module=bs-auto-status)

- 按 gpc 试点模板接入：index.html 引入 `https://cdn.chehejia.com/sdk/zeus.js`(内网 CDN);新增 `src/zeus.ts` 单例封装(`appid='li-bs-auto-status'`(各端独立 appid)、`module='bs-auto-status'`、`intranetOnly: true`、`spa: true`、`plugins: ['error','performance']`、`track: false`、env 按 `VITE_API_ENV/MODE` 映射、构造器异常隔离不打断登录);身份获取成功后调用一次 `initZeus`。
- 身份适配:`UserProfile` 补 `ldapName`,`normalizeUserProfile` 从 `user.ldap_name` 透传;`App.tsx loadAuth` 成功后 `initZeus(nextProfile)`;unknown/readonly 占位身份不初始化 Zeus。合同测试位于包根(沿用 permission-regression 布局)。
- 新增 `test:zeus-contracts`(typescript transpileModule 方案,不依赖 esbuild):覆盖 SDK 异常隔离、prod 环境映射、intranetOnly、module 名与身份参数(uvKey/liUsername/liOpenId)。
- 验证:test:zeus-contracts、type-check、build 通过。
