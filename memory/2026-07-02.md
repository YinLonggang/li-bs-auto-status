# 2026-07-02

## Auto Status 共享盘配置下线

- 侧边栏删除“附件共享盘”入口。
- 前端删除 shared-storage profile 类型、API client、保存/测试逻辑和配置面板。
- 附件对象仍走后端附件服务，后端全局 shared-storage app 目录固定为 `li-bs-auto-status`。

## 长期边界

- Auto Status SPA 不再调用 `/api/shared-storage/v1/profiles/li_bs_auto_status/` 或 scope test endpoint。
- 共享盘连接、认证和传输参数只在 `li_sicar` 主页基础配置区维护。
