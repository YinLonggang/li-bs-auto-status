# 2026-07-22

## Workbench Shell 统一与遮挡复核

- 外壳统一为 `LI-SICAR · BIW CLOUD LINE / 理想BIW云上产线 / 项目状态中心`，桌面侧栏支持 288px 与 80px rail 切换，主题统一写入 `li_sicar_theme`。
- 顶栏保留应用级返回门户、主题和用户状态；展开侧栏用户卡把角色徽章移入文本列，避免常见邮箱被徽章横向挤压截断。
- `npm run type-check`、`npm run build`、`npm run permission-regression` 通过；权限回归因缺少场景 Cookie 按脚本规则 SKIP。
